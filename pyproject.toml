[project]
name = "scout"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
  "docling>=2",
  "lancedb>=0.20",
  "llama-cpp-python>=0.3",
  "pypdfium2>=5",
]

[project.optional-dependencies]
build = [
  "pyinstaller>=6",
]
